// ARROW FUNCTIONS

// 1. Syntax 

const sayHello = () => {
    console.log("Hey!");
};

const add = (num1, num2) => {
    return num1 + num2;
};

const addV2 = (num1, num2) => num1 + num2;  // This is One-Liner approach

console.log(addV2(3,4));
